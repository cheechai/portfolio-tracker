[build]
builder = "nixpacks"
buildCommand = "npm install && npm rebuild && npm run build"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_22"]

[deploy]
startCommand = "npx serve dist -p $PORT"
